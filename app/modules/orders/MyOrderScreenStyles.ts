import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  yourLastOrders: {
    fontSize: 16,
    marginBottom: 16,
  },
  yourLastOrdersWeb: {
    fontSize: 14,
    marginBottom: 12,
    fontWeight: '500',
  },
});

export default styles;
